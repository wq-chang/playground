// Package config provides application configuration loading
// from environment variables. It validates required settings
// and builds structured configuration objects used by the app.
package config
